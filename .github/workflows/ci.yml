name: trying
on: [push, pull_request]

jobs:
  job:
    name: Basic (${{ matrix.triplet }})
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - triplet: x64-windows
            vcpkgCommitId: '801084007159e0b1b4c95134e754d876e688ebed'
            vcpkgPackages: 'taglib libpng'

    steps:
      - uses: actions/checkout@v1
        with:
          submodules: true

      - uses: lukka/get-cmake@latest
      - name: Restore artifacts, or run vcpkg, build and cache artifacts
        uses: lukka/run-vcpkg@main
        id: runvcpkg
        with:
          vcpkgArguments: '${{ matrix.vcpkgPackages }}'
          vcpkgTriplet: '${{ matrix.triplet }}'
          vcpkgDirectory: '${{ runner.workspace }}/b/vcpkg'
          vcpkgGitCommitId: '${{ matrix.vcpkgCommitId }}'
          
      - name: Prints outputs of run-vcpkg task
        run: |
          ${{ runner.workspace }}/b/vcpkg/vcpkg.exe export --triplet=${{ matrix.triplet }} --7zip taglib
          dir ${{ runner.workspace }}/b/vcpkg/
        
      - name: Run CMake+Ninja with triplet (cmd)
        uses: lukka/run-cmake@main
        id: runcmake_cmd
        with:
          cmakeGenerator: 'Ninja'
          cmakeBuildType: 'Release'
          useVcpkgToolchainFile: true
          buildWithCMakeArgs: '-- -v'
          #cmakeAppendedArgs: -DX_VCPKG_APPLOCAL_DEPS_INSTALL=ON
          vcpkgTriplet: ${{ steps.runvcpkg.outputs.RUNVCPKG_VCPKG_TRIPLET_OUT }}
          buildDirectory: '${{ runner.workspace }}/b/ninja/'
      - name: Install
        run: |
          cd '${{ runner.workspace }}/b/ninja/'
          cmake --install . --prefix out
          cpack -G WIX .
      - name: Archive code coverage results
        uses: actions/upload-artifact@v2
        if: always()
        with:
          name: Build Dir
          path: |
            ${{ runner.workspace }}/b/ninja/
            ${{ runner.workspace }}/b/vcpkg/installed/